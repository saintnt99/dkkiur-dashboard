# Заменить на свой пароль (минимум 12 символов, латиница+цифры+спецсимволы)
SITE_PASSWORD=replace-with-strong-password

# Сгенерировать: openssl rand -hex 32
JWT_SECRET=replace-with-64-hex-chars

# Сгенерировать: openssl rand -hex 16
POSTGRES_PASSWORD=replace-with-32-hex-chars

# Домен, по которому будет открываться дашборд.
# Варианты:
#   1) свой купленный домен:           dkkiur.example.ru
#   2) sslip.io на IP:                 5.42.113.80.sslip.io
#   3) если хочешь сразу IP (без TLS): http://5.42.113.80 (тогда закомментируй HTTPS-блок)
DOMAIN=5.42.113.80.sslip.io
